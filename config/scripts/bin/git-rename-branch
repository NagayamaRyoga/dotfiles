#!/usr/bin/env bash
set -u
\command git rev-parse || exit 1

old_branch="$(\command git branch --show-current)"
new_branch="$(\command ped <<<"$old_branch")"
\command git branch -m "$old_branch" "$new_branch"
